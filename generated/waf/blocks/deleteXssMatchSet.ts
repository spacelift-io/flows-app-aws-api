import { AppBlock, events } from "@slflows/sdk/v1";
import { WAFClient, DeleteXssMatchSetCommand } from "@aws-sdk/client-waf";

const deleteXssMatchSet: AppBlock = {
  name: "Delete Xss Match Set",
  description: "This is AWS WAF Classic documentation.",
  inputs: {
    default: {
      config: {
        region: {
          name: "Region",
          description: "AWS region for this operation",
          type: "string",
          required: true,
        },
        XssMatchSetId: {
          name: "Xss Match Set Id",
          description:
            "The XssMatchSetId of the XssMatchSet that you want to delete.",
          type: "string",
          required: true,
        },
        ChangeToken: {
          name: "Change Token",
          description:
            "The value returned by the most recent call to GetChangeToken.",
          type: "string",
          required: true,
        },
      },
      onEvent: async (input) => {
        const { region, ...commandInput } = input.event.inputConfig;

        const client = new WAFClient({
          region: region,
          credentials: {
            accessKeyId: input.app.config.accessKeyId,
            secretAccessKey: input.app.config.secretAccessKey,
            sessionToken: input.app.config.sessionToken,
          },
        });

        const command = new DeleteXssMatchSetCommand(commandInput as any);
        const response = await client.send(command);

        await events.emit(response || {});
      },
    },
  },
  outputs: {
    default: {
      name: "Delete Xss Match Set Result",
      description: "Result from DeleteXssMatchSet operation",
      possiblePrimaryParents: ["default"],
      type: {
        type: "object",
        properties: {
          ChangeToken: {
            type: "string",
            description:
              "The ChangeToken that you used to submit the DeleteXssMatchSet request.",
          },
        },
        additionalProperties: true,
      },
    },
  },
};

export default deleteXssMatchSet;
